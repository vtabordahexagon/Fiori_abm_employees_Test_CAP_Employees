_schema-version: "3.2"
ID: fioriabmempleados
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: fioriabmempleados-destination-content
  type: com.sap.application.content
  requires:
  - name: fioriabmempleados-destination-service
    parameters:
      content-target: true
  - name: fioriabmempleados-repo-host
    parameters:
      service-key:
        name: fioriabmempleados-repo-host-key
  - name: fioriabmempleados-uaa
    parameters:
      service-key:
        name: fioriabmempleados-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: fioriabmempleados_html_repo_host
          ServiceInstanceName: fioriabmempleados-html5-srv
          ServiceKeyName: fioriabmempleados-repo-host-key
          sap.cloud.service: fioriabmempleados
        - Authentication: OAuth2UserTokenExchange
          Name: fioriabmempleados_uaa
          ServiceInstanceName: fioriabmempleados-xsuaa-srv
          ServiceKeyName: fioriabmempleados-uaa-key
          sap.cloud.service: fioriabmempleados
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: fioriabmempleados-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: fioriabmempleados-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - fioriabmempleados.zip
      name: fioriabmempleados
      target-path: resources/
- name: fioriabmempleados
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: fioriabmempleados-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: fioriabmempleados-destination-service
    service-plan: lite
- name: fioriabmempleados-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: fioriabmempleados-xsuaa-srv
    service-plan: application
- name: fioriabmempleados-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: fioriabmempleados-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
